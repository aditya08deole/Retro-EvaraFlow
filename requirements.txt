# ============================================================
# RetroFit Image Capture Service v2.1 - Dependencies
# Cloud Processing Architecture (No Edge ML)
# Optimized for Raspberry Pi Zero W (512MB RAM, ARMv6)
# ============================================================

# Core Image Capture
# NOTE: picamera is for legacy camera stack (Buster/Bullseye-legacy).
# On Bookworm, picamera2 is pre-installed via system packages.
picamera==1.13

# Numerical computing (required by OpenCV)
numpy==1.19.5

# Computer Vision (ArUco marker detection and ROI extraction)
# Installed separately in install.sh to bypass PyPI hash issues on ARMv6.
# Target version: opencv-contrib-python-headless==4.5.1.48

# GPIO Control
RPi.GPIO==0.7.1

# Cloud Upload & HTTP
requests==2.26.0
urllib3==1.26.7

# Utilities
python-dateutil==2.8.2
pytz==2021.3
